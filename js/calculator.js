let button = document.querySelector(".calcForm__item--btn");
button.addEventListener("click", calcFinalPrice);


function calcFinalPrice(e) {
    e.preventDefault();
    let item = document.querySelectorAll(".calcForm__item");
    item.forEach();
    console.log(item);
}